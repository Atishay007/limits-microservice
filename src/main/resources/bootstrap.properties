#This is the service name.
spring.application.name=limits-service

##Every micorservice application will run on different port.
server.port=8082

#limits-service.maxconfiguration=99
#limits-service.minconfiguration=9

#This is the URL where spring cloud config server is running.
spring.cloud.config.uri=http://localhost:8888

#whch default env. value to pick.
spring.profiles.active=dev